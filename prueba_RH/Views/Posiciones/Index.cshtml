@model IEnumerable<prueba_RH.Models.Posicione>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NPosicion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionPosicion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdDepartamentos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NPosicion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionPosicion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDepartamentos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
