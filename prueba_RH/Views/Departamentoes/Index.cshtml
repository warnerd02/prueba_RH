@model IEnumerable<prueba_RH.Models.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h1>department</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NDepartamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionDepartamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NDepartamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionDepartamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
